services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: test-docker
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
    ports:
      - "0.0.0.0:4001:4000"